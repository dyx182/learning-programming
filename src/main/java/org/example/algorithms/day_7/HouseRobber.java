package org.example.algorithms.day_7;

public class HouseRobber {

    /**
     * Задача — House Robber.
     *
     * Ты грабишь дома на улице, выстроенной в ряд. В массиве nums[i] —
     * сколько денег лежит в i-м доме. Нельзя ограбить ДВА СОСЕДНИХ дома
     * подряд (иначе сработает сигнализация). Верни МАКСИМАЛЬНУЮ сумму,
     * которую можно унести, не ограбив соседние дома.
     *
     * ЧЕМУ УЧИТ ЭТА ЗАДАЧА:
     * Тема — Динамическое программирование (DP), продолжение дня 6.
     * Это классика «take or skip»: для каждого дома есть выбор —
     * 1) ограбить его (тогда предыдущий дом грабить нельзя, берём деньги из
     *    дома i-2), либо
     * 2) пропустить его (тогда остаёмся с результатом для дома i-1).
     * Отсюда рекуррентность: dp[i] = max(dp[i-1], dp[i-2] + nums[i]).
     * Ключевые понятия: состояние, переход, выбор «взять/не взять»,
     * оптимизация по памяти (хранить только два последних значения).
     *
     * ГДЕ ИСКАТЬ ТЕОРИЮ: «House Robber», «dynamic programming take or skip»,
     * «maximum sum without adjacent», «bottom-up DP», «оптимизация по памяти».
     *
     * ПОДСКАЗКА (не решение):
     * 1) Выпиши руками для малых массивов и заметь закономерность.
     *    Например, [1,2,3,1] -> 4 (берём 1-й и 3-й дом), [2,7,9,3,1] -> 12.
     * 2) Заведи два числа: сколько мы можем унести, рассматривая дома до i-1
     *    (prev1) и до i-2 (prev2). На каждом шаге
     *    текущее = max(prev1, prev2 + nums[i]).
     * 3) Затем сдвинь: prev2 = prev1, prev1 = текущее.
     * 4) Пройдись по всем домам один раз — получишь O(n) времени и O(1) памяти.
     *
     * Все тест-кейсы (вход -> ожидание):
     * - [1,2,3,1]   -> 4
     * - [2,7,9,3,1] -> 12
     * - [2,1,1,2]   -> 4
     * - [5,3,4,11,2]-> 16
     * - [1]         -> 1
     * - []          -> 0
     * - [0]         -> 0
     */
    public static int rob(int[] nums) {
        return 0;
    }
}
