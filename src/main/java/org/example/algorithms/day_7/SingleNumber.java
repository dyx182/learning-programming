package org.example.algorithms.day_7;

public class SingleNumber {

    /**
     * Задача — Single Number.
     *
     * Дан непустой массив целых чисел nums, в котором КАЖДОЕ число встречается
     * дважды, и только ОДНО число встречается один раз. Найди это единственное
     * число.
     *
     * ЧЕМУ УЧИТ ЭТА ЗАДАЧА:
     * Тема — битовые операции (XOR) и работа с множествами.
     * Ключевой трюк: свойство XOR (побитового исключающего ИЛИ):
     * a ^ a = 0, a ^ 0 = a, и порядок не важен (a ^ b ^ a = b).
     * Если проXORить все числа массива, одинаковые пары дадут 0,
     * а единственное число останется. Сложность O(n) времени и O(1) памяти.
     *
     * ГДЕ ИСКАТЬ ТЕОРИЮ: «single number leetcode», «XOR bitwise trick»,
     * «побитовые операции», «find the element that appears once».
     *
     * ПОДСКАЗКА (не решение):
     * 1) Обычный путь — использовать HashSet или HashMap (счётчик вхождений),
     *    но это O(n) памяти.
     * 2) Подумай про XOR: если сложить все числа по XOR, что останется?
     * 3) Попробуй на бумаге: [2, 2, 1] -> 2^2^1 = 0^1 = 1.
     * 4) Другой вариант — сумма всех чисел минус сумма «уникальных» умножить
     *    на 2, но это рискует переполнением; XOR безопаснее.
     *
     * Все тест-кейсы (вход -> ожидание):
     * - [2,2,1]    -> 1
     * - [4,1,2,1,2]-> 4
     * - [1]        -> 1
     * - [0]        -> 0
     */
    public static int singleNumber(int[] nums) {
        return 0;
    }
}
