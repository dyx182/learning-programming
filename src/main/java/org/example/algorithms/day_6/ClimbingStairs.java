package org.example.algorithms.day_6;

public class ClimbingStairs {

    /**
     * Задача — Climbing Stairs.
     *
     * Ты поднимаешься по лестнице из n ступеней. За один шаг можно подняться
     * на 1 или на 2 ступени. Верни, сколькими РАЗНЫМИ способами можно добраться
     * до вершины (считая комбинации шагов 1 и 2).
     *
     * ЧЕМУ УЧИТ ЭТА ЗАДАЧА:
     * Тема — Динамическое программирование (DP) и рекурсия.
     * Это классика «чисел Фибоначчи»: число способов добраться до ступени n
     * равно сумме способов добраться до n-1 и n-2, потому что на последний шаг
     * ты попадаешь либо с n-1 (шаг 1), либо с n-2 (шаг 2).
     * Ключевые понятия: базовый случай (base case), рекуррентное соотношение,
     * перекрывающиеся подзадачи, мемоизация (запоминание), подходы
     * сверху-вниз (top-down: рекурсия + memo) и снизу-вверх (bottom-up: цикл).
     *
     * ГДЕ ИСКАТЬ ТЕОРИЮ: «динамическое программирование», «числа Фибоначчи»,
     * «рекурсия + мемоизация», «bottom-up vs top-down».
     *
     * ПОДСКАЗКА (не решение):
     * 1) Начни с малых n и выпиши ответы руками: n=1->1, n=2->2, n=3->3, n=4->5.
     *    Заметь закономерность — это 1,2,3,5,8... (числа Фибоначчи).
     * 2) Ключевой вопрос: «Если я стою на ступени n, где я стоял на предыдущем
     *    шаге?» Ответ: либо на n-1, либо на n-2. Отсюда
     *    способов(n) = способов(n-1) + способов(n-2).
     * 3) Сначала попробуй рекурсию (функция вызывает саму себя), потом добавь
     *    запоминание уже посчитанных значений (memo), затем — итеративный цикл.
     *    Сравни сложность: наивная рекурсия O(2^n), с memo O(n),
     *    итеративно O(n) времени и O(1) памяти.
     *
     * Все тест-кейсы (вход -> ожидание):
     * - n = 1 -> 1
     * - n = 2 -> 2
     * - n = 3 -> 3
     * - n = 4 -> 5
     * - n = 5 -> 8
     * - n = 6 -> 13
     * - n = 0 -> 1
     */
    public static int climbStairs(int n) {
        return 0;
    }
}
